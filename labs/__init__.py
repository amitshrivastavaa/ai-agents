"""labs/ — an overnight research lab of wild, ambitious AI-agent MVPs.

Each subpackage is a self-contained MVP inspired by a trending idea in the
agent space (multi-agent debate firms, agents with persistent memory,
agentic-security harnesses, ...). Every MVP runs **offline with the Python
standard library alone** so it works on a fresh clone with no API key and no
dependencies, and upgrades to a real model when ``ANTHROPIC_API_KEY`` is set.

See ``labs/README.md`` for the index and ``labs/PROGRESS.md`` for the build log.
"""
